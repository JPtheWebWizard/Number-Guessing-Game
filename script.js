let running = true;
const minNum = 1;
const maxNum = 100;
let guess;
let attempts = 0;

const answer;

while (true) {
    
    




    running = false;
}

function randomNumGen(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}